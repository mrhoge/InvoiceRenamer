# TODO

機能追加・改善の候補リスト。上から優先度順。

## 優先度: 高

- [x] **古いログの自動削除**（実装済み）
  起動時に `logs/` 内の30日より古いログを削除する。保持日数は `constants.py` の `LOG_RETENTION_DAYS` で変更可能。
- [x] **依存ライブラリの脆弱性チェックの自動化**（実装済み）
  `.github/dependabot.yml` を追加。push後、GitHubリポジトリの Settings → Code security で Dependabot alerts が有効になっていることを一度確認すること。

## 優先度: 中

- [ ] **リネーム履歴の表示とアンドゥ**
  「いつ・どのファイルを・何にリネームしたか」の一覧画面。renamed フォルダへのコピー方式なので元ファイルは残るが、後から対応関係を確認できると安心感が増す。
- [x] **OCR前処理による精度向上**（実装済み）
  比較実験の結果「常時前処理はTesseract 5では逆効果」と判明したため、**元画像でのOCRが失敗したときだけ補正画像（ノイズ除去・拡大）で再試行する**フォールバック方式で実装（`logic/ocr_preprocess.py`）。設定項目なし・プレビュー表示への影響なし・元画像で読めるケースへのリグレッションなし。
- [ ] **config.toml の設定画面（GUI）**
  現在は y_coordinate_tolerance 等を手動編集する必要がある。設定ダイアログから変更できれば README の説明も減らせる。

## 優先度: 低

- [x] **renamed フォルダを開く操作**（実装済み）
  画面上のボタンではなく、ファイルメニュー（「リネーム後ファイル保存先を開く」「終了」）として実装。誤操作を防ぎつつ画面のボタンを増やさない方針。
- [ ] **連続処理モード**（リネーム確定後、自動的に次のPDFを開く）
- [ ] **Mac での動作確認**（README に記載済みの既知課題）
